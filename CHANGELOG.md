# 변경 이력

버전별로 사용자에게 보이는 변경 사항을 기록합니다.
기능 추가와 동작 변경은 같은 작업에서 이 파일을 갱신합니다.
다음 릴리스가 정해지기 전에는 `Unreleased`에 기록하고, 릴리스 시 버전과 날짜를 붙입니다.

## [Unreleased]

### 문서

- Codex project-init 기준의 문서 목차, 아키텍처, 온보딩, 기여 지침, 구현 참조와 릴리스 런북을 추가합니다.
- 배포 패키지에 `docs/`를 포함해 README의 문서 링크를 설치 파일에서도 사용할 수 있게 합니다.

## [0.5.2] - 2026-09-13

### 수정

- 기본 `start`와 `watch`에서 마우스 캡처를 꺼 드래그로 바로 텍스트를 선택·복사할 수 있도록 합니다. Codex의 마우스 모드 요청, HUD 갱신, 선택 모드 종료 후에도 기본 드래그 선택을 유지합니다.
- 휠 스크롤과 Codex 마우스 입력은 `--mouse` 또는 설정의 `"mouse": true`로 켤 수 있습니다. `--no-mouse`는 저장된 캡처 설정을 해제하며, `Alt+M`은 화면을 고정하고 캡처를 잠시 멈추는 기능을 유지합니다.

## [0.5.1] - 2026-09-13

### 수정

- 에이전트 목록과 요약에는 실행 중인 에이전트만 표시합니다. 완료·오류·중단·종료된 에이전트는 숨깁니다.
- 실행 중인 에이전트가 없으면 에이전트 제목과 요약도 생략하고, 줄어든 HUD 높이에 맞춰 Codex 표시 공간을 돌려줍니다. 에이전트를 다시 실행하면 목록에 표시됩니다.

## [0.5.0] - 2026-09-13

### 추가

- `Alt+L`로 실행 중인 HUD의 한글/영문을 즉시 전환합니다. 현재 실행에만 적용하며, `start`와 대화형 `watch`에서 사용할 수 있습니다.
- `Alt+M`으로 텍스트 선택 모드를 전환합니다. 마우스 캡처와 화면 갱신을 잠시 멈춰 드래그로 텍스트를 선택·복사할 수 있고, 다시 누르면 최신 화면으로 돌아옵니다. Codex 작업과 세션 기록 읽기는 계속 진행됩니다.

### 수정

- 매 화면 갱신마다 마우스 추적 모드를 다시 설정하던 동작을 제거해 터미널의 텍스트 선택을 방해하지 않도록 합니다.
- 언어를 전환할 때 캐시된 상태를 즉시 다시 표시하고, 진행 중인 갱신이 이전 언어로 화면을 되돌리지 않도록 합니다.
- 붙여넣은 텍스트에 포함된 단축키 바이트는 HUD 명령으로 처리하지 않습니다.
- 창 포커스 알림이 선택 모드를 해제하거나 불완전한 터미널 응답이 `Ctrl+C`를 가로채지 않도록 합니다.

## [0.4.0] - 2026-09-13

### 추가

- 현재 디렉터리 바로 옆에 Git 상태를 묶어 표시합니다. 브랜치, 변경·미추적 파일 수, ahead/behind와 변경 없는 상태를 확인할 수 있습니다.
- Git 정보를 읽을 수 없거나 저장소가 아닌 디렉터리에서는 `Git —`를 표시합니다. `--no-git`이면 Git 표시를 생략합니다.

### 개선

- 토큰 행의 경과 시간에 `세션`/`Session` 라벨을 붙여 세션 실행 시간을 구분합니다. 시작 시각이 없으면 `—`를 표시하며 완료·중단 상태의 시간은 마지막 기록 시각을 기준으로 유지합니다.
- HUD와 설치 플러그인·단독 설치 스킬의 배포 버전을 `0.4.0`으로 맞춥니다.

## [0.3.0] - 2026-09-13

### 추가

- **에이전트 상세 정보**: `full` 화면에 각 에이전트의 이름, 역할, 상태를 표시합니다. 실행 중인 에이전트를 먼저 보여 주고 완료·오류·중단·종료 상태도 함께 확인할 수 있습니다.
- **로드한 스킬 정보**: 성공한 `SKILL.md` 읽기에서 확인한 스킬 이름과 개수를 도구 행 바로 아래 한 줄 목록으로 표시합니다. 같은 이름은 중복 없이 최근에 읽은 순서로 쉼표로 구분하며, 너비를 넘으면 말줄임표로 표시합니다. `essential`에서는 개수만 표시합니다.
- **사용한 플러그인 정보**: 읽은 플러그인 스킬의 캐시 경로에서 이름, 버전과 마켓플레이스를 확인해 표시합니다. 같은 플러그인의 여러 스킬은 하나로 집계하며, `essential`에서는 개수만 표시하고 JSON 상태에도 정보를 제공합니다.
- 상단에 현재 세션의 승인 정책과 검토 주체를 표시합니다.

### 개선

- `도구 → 스킬 → 플러그인 → 에이전트 → 계획` 순서로 관련 정보를 모아 표시합니다.
- 긴 에이전트·플러그인 목록에 맞춰 HUD 높이를 조절하고, 마우스 휠과 키보드로 목록을 스크롤할 수 있습니다.
- 에이전트·스킬·플러그인은 각각 최근 40개까지 추적하며, `minimal`에서는 상세 목록을 생략합니다.

### 배포와 문서

- HUD, 설치 플러그인과 단독 설치 스킬의 배포 버전을 `0.3.0`으로 맞춥니다.
- 버전별 변경 이력과 기능 추가 시 갱신 기준을 추가하고, npm 패키지에 `CHANGELOG.md`를 포함합니다.

## [0.2.0] - 2026-09-09

### 추가

- `codex-hud start`로 한 터미널에서 Codex 아래에 전체 HUD를 고정해 표시하는 inline 실행을 제공합니다.
- 터미널 크기 변경, Codex 입력 전달, 마우스 휠 스크롤백과 종료 시 화면·커서·입력 모드 복원을 지원합니다.
- HUD 실행 패키지를 동봉한 설치 스킬과 플러그인, 선택적 Bash/Zsh 자동 실행 설정을 제공합니다. 당시 설치 플러그인의 버전은 `0.1.0`입니다.

### 변경

- 기본 실행에서 tmux 의존성을 제거하고, 기존 방식은 `codex-hud start --tmux`로 제공합니다.
- inline PTY 가용성을 `doctor`에서 확인하고, PTY가 필요 없는 명령은 네이티브 모듈 없이 사용할 수 있습니다.

## [0.1.0] - 2026-09-09

### 추가

- 로컬 Codex 세션 기록을 읽는 실시간 HUD와 `watch`, `status`, `demo`, `setup`, `doctor` 명령을 제공합니다.
- 모델·추론 수준, 컨텍스트, 사용 한도, 누적 토큰, 도구 실행, 에이전트 상태 집계, 작업 계획과 Git 상태를 표시합니다.
- 별도 터미널 모니터링과 tmux에서 Codex와 HUD를 함께 실행하는 방식을 지원합니다.
- 전체·간단·최소 표시 모드, 한글·영문, 색상·ASCII와 너비 설정을 제공합니다.
